node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Capacitor / Android build artifacts
app/dist/
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/.idea/
android/app/release/
*.keystore
!debug.keystore
!skyhaven-debug.keystore
# google-services.json contains only public client identifiers (api key
# is package + SHA-1 restricted; appId / oauth client id are embedded
# in the APK either way). Safe to commit so CI debug builds get OAuth
# wired up. Service-account JSONs and secrets remain ignored separately.
!android/app/google-services.json
GoogleService-Info.plist

# iOS (deferred)
ios/App/Pods/
ios/App/build/
ios/App/DerivedData/

# Firebase
.firebaserc.local
functions/node_modules/
functions/lib/
functions/.env

# IDE
.vscode/
*.swp

# Raw data inputs (regenerate via scripts/build-airports.mjs)
scripts/data/*.csv
scripts/data/*.geojson
