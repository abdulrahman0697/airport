{
  "conditions": [],
  "parameters": {
    "econ_global_yield_mult": {
      "defaultValue": { "value": "1.0" },
      "valueType": "NUMBER",
      "description": "Global revenue multiplier applied to all routes."
    },
    "offline_rate_pct": {
      "defaultValue": { "value": "0.5" },
      "valueType": "NUMBER",
      "description": "Offline-earning fraction of online rate (0..1)."
    },
    "offline_cap_hours": {
      "defaultValue": { "value": "1" },
      "valueType": "NUMBER",
      "description": "Maximum offline accrual window in hours. Matches the shipped 1h cap (idle income is a comeback nudge, not an overnight engine)."
    },
    "fuel_demand_mult": {
      "defaultValue": { "value": "2.0" },
      "valueType": "NUMBER",
      "description": "Multiplies total fleet fuel demand. 2.0 since the Arrivals & Time update restored the fuel gate after strict-linear timing halved burn rate."
    },
    "fuel_contract_cost_mult": {
      "defaultValue": { "value": "1.0" },
      "valueType": "NUMBER"
    },
    "fuel_capacity_cost_mult": {
      "defaultValue": { "value": "1.0" },
      "valueType": "NUMBER"
    },
    "condition_decay_rate": {
      "defaultValue": { "value": "1.0" },
      "valueType": "NUMBER"
    },
    "repair_cost_mult": {
      "defaultValue": { "value": "1.0" },
      "valueType": "NUMBER"
    },
    "hub_bonus_per_level": {
      "defaultValue": { "value": "0.05" },
      "valueType": "NUMBER"
    },
    "interstitial_min_interval_seconds": {
      "defaultValue": { "value": "180" },
      "valueType": "NUMBER"
    },
    "rewarded_fuel_amount": {
      "defaultValue": { "value": "500" },
      "valueType": "NUMBER"
    },
    "min_supported_app_version": {
      "defaultValue": { "value": "0.1.0" },
      "valueType": "STRING"
    },
    "feature_flag_cargo": { "defaultValue": { "value": "true" }, "valueType": "BOOLEAN" },
    "feature_flag_leaderboards": { "defaultValue": { "value": "false" }, "valueType": "BOOLEAN" },
    "feature_flag_social": { "defaultValue": { "value": "false" }, "valueType": "BOOLEAN" },
    "feature_flag_events": { "defaultValue": { "value": "false" }, "valueType": "BOOLEAN" },
    "event_schedule": {
      "defaultValue": { "value": "[]" },
      "valueType": "STRING",
      "description": "JSON array of {id, kind, regionId, startsAtMs, endsAtMs, announceLeadMs} scheduled events. Phase 13 lets us launch events without an app update."
    }
  },
  "version": {
    "description": "Phase 0 defaults — economy live-tuning lands as phases come online."
  }
}
